#!/bin/bash
   /**
    *Write a C program that prints exactly "Programming is like building a multilingual puzzle, followed by a new line using the function puts and end with value 0
    */
int main(void)
{
  char str[] = "Programming is like building a multilingual puzzle";
  puts(str);
  return (0);
}
